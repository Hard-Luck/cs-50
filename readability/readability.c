#include <stdio.h>
#include <cs500.h>

string text = get_string("Text: ")

int main(void)
{

}

int count_letters(string text)
{
    int count = 0;
    for (int i; string[i], i++)
    {
        if 
    }
}