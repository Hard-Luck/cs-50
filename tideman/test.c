bool test(int n, int m)
{
    if(n == m)
    {
        for(int i = 0, i < candidate_count; i++)
        {
            if preference[n][i] = true
            {
                return test(n, m);
            }
        }
    }
}